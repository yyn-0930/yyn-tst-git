spring:
  datasource:
    driver-class-name: org.h2.Driver  
    url: jdbc:h2:mem:testdb?characterEncoding=UTF8;MODE=PostgreSQL;
    username: sa    
    password:  
  jpa:
    show-sql: true
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    
###################################
# API connection info
###################################
app:
  service-url:
    url: http://localhost:8001
    port: 8001