package jp.co.maruzenshowa.malos.common.utils;

import static org.junit.jupiter.api.Assertions.assertEquals;
import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.springframework.test.context.junit.jupiter.SpringExtension;

@ExtendWith(SpringExtension.class)
class MessageUtilsTest {
	@Test
	void testGetMessage() {
//		Map<String, String> args = new ConcurrentHashMap<>();
//		args.put("name", "column");
//		args.put("min", "2");
//		args.put("max", "10");
//		String expectedValue = "column is between 2 and 10.";
//		assertEquals("Expected column value to be between 2 and 10, but it was not.", expectedValue , MessageUtils.getMessage("A9099D", args));
	}
}
